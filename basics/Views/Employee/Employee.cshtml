@model List<Employee>

<div class="display-6">
    Index 1
</div>

@foreach (Employee msg in Model)
    {
        <div class="lead my-2">
            <h6>@msg.FullName</h6>
            <p>@msg.Id</p>
            <p><strong>Age:</strong>@msg.Age</p>
            <hr />
        </div>
    }